apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /ex1
        pathType: Prefix
        backend:
          service:
            name: cicd-service
            port:
              number: 8081
              